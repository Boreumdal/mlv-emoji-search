# Emoji Searcher using Open Emoji API

### How to install:
- Download and extract
- Open terminal and run `npm i`
- After installaation of dependencies, run the program by executing `npm run dev`

### Made by:
- React
- Tailwind